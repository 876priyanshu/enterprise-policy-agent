global:
  scrape_interval: 10s # How often to scrape targets

scrape_configs:
  - job_name: 'fastapi_backend'
    static_configs:
      # "api" is the name of the FastAPI service in our docker-compose file
      # "8000" is the port it runs on inside the Docker network
      - targets: ['api:8000']